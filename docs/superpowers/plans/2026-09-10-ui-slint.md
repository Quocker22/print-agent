# Print-agent UI Slint Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development hoặc superpowers:executing-plans để thực thi từng task. Steps dùng checkbox (`- [ ]`).

**Goal:** Thay UI print-agent từ egui (chết trên GPU ảo VMware) sang Slint software-render, giữ nguyên lõi net/print/config.

**Architecture:** Lõi (net.rs/printing.rs/config.rs/state.rs/job.rs) tách sạch khỏi UI qua `Arc<Mutex<TrangThaiChung>>` — chỉ viết lại `ui.rs` + thêm file .slint/build.rs/view_model.rs/taskbar_win.rs, đổi deps egui→slint. UI theo thiết kế print-agent.pen.

**Tech Stack:** Rust, Slint 1.8 (winit backend, software renderer), tray-icon 0.24, rust_socketio, windows-rs (ẩn taskbar).

**Spec:** `docs/spec-ui-slint.md`

## Global Constraints

- **Ngôn ngữ chạy: Windows** (máy shop VMware SVGA, không GPU). Build+test trên máy Win qua WinRM (192.168.18.207) — Mac chỉ viết code + cargo check phần không phụ thuộc Windows API.
- **Slint software renderer BẮT BUỘC**: set `SLINT_BACKEND=winit-software` (hoặc ép trong code) — GPU-render chết trên VMware.
- **Giữ chữ ký `pub fn chay_ui(cfg: Arc<Config>, trang_thai: Arc<Mutex<TrangThaiChung>>) -> Result`** để main.rs gần như không đổi.
- **KHÔNG đổi**: net.rs, printing.rs, state.rs, job.rs, giao thức socket.io.
- **"Mã shop" = org_id**; token = hằng nhúng binary (`const AGENT_TOKEN`).
- **Tray-only, ẩn taskbar Windows** qua Win32 WS_EX_TOOLWINDOW.
- **Deploy qua git** (push GitHub, máy Win pull+build) — KHÔNG cp lẻ file vào chỗ chạy.
- Font Việt: nhúng assets/font-regular.ttf (Be Vietnam Pro) vào Slint.

---

## File Structure

- `Cargo.toml` — bỏ eframe/egui/wgpu; thêm slint, slint-build (build-dep), windows (ẩn taskbar). Giữ tray-icon/image/rust_socketio/serde/base64/anyhow.
- `build.rs` (mới) — `slint_build::compile("ui/print-agent.slint")`.
- `ui/print-agent.slint` (mới) — UI khai báo theo Pencil.
- `src/view_model.rs` (mới, THUẦN, test được) — struct + hàm map `TrangThaiChung`/`Config` → dữ liệu hiển thị (chuỗi trạng thái, list job, màu badge).
- `src/taskbar_win.rs` (mới) — Win32 ẩn cửa sổ khỏi taskbar (cfg Windows).
- `src/ui.rs` (viết lại) — Slint: dựng window, tray-icon, vòng lặp đọc trang_thai, nối nút Lưu/In thử/menu tray, gọi taskbar_win.
- `src/config.rs` (sửa nhẹ) — token thành hằng, "mã shop"=org_id.
- `src/main.rs` (không đổi logic, chỉ giữ gọi chay_ui).
- Lõi net/printing/state/job.rs — KHÔNG đụng.

---

## Task 1: view_model — logic thuần map trạng thái → hiển thị (TDD)

**Files:**
- Create: `src/view_model.rs`
- Modify: `src/main.rs` (thêm `mod view_model;`)
- Test: trong `src/view_model.rs` (`#[cfg(test)]`)

**Interfaces:**
- Consumes: `state::TrangThaiChung { da_noi: bool, jobs: Vec<JobLog>, thong_bao_cuoi: Option<String> }`, `state::JobLog { so_hoa_don, khach: Option<String>, trang_thai: String, luc: String }`, `config::Config`.
- Produces: `pub struct ViewModel { pub trang_thai_text: String, pub da_noi: bool, pub server: String, pub may_in: String, pub jobs: Vec<JobRow> }`; `pub struct JobRow { pub so_hoa_don: String, pub khach: String, pub badge: String, pub da_in: bool }`; `pub fn build_view_model(cfg: &Config, t: &TrangThaiChung) -> ViewModel`.

- [ ] **Step 1: Viết test fail**

```rust
#[cfg(test)]
mod tests {
    use super::*;
    use crate::state::{TrangThaiChung, JobLog};
    use crate::config::Config;

    fn cfg() -> Config {
        Config { server_url: "zalocrm.incokit.com".into(), token: "".into(),
                 org_id: "shop1".into(), printer_name: "HP 4003".into(),
                 tray: "tray-1".into(), paper_size: "A5".into() }
    }

    #[test]
    fn da_noi_ra_text_xanh() {
        let t = TrangThaiChung { da_noi: true, jobs: vec![], thong_bao_cuoi: None };
        let vm = build_view_model(&cfg(), &t);
        assert_eq!(vm.trang_thai_text, "Đã kết nối");
        assert!(vm.da_noi);
        assert_eq!(vm.may_in, "HP 4003 · A5");
    }

    #[test]
    fn mat_noi_ra_text_do() {
        let t = TrangThaiChung { da_noi: false, jobs: vec![], thong_bao_cuoi: None };
        assert_eq!(build_view_model(&cfg(), &t).trang_thai_text, "Mất kết nối");
    }

    #[test]
    fn job_map_badge_dung() {
        let t = TrangThaiChung { da_noi: true, thong_bao_cuoi: None, jobs: vec![
            JobLog { so_hoa_don: "INV/1".into(), khach: Some("Anh A".into()), trang_thai: "da_in".into(), luc: "10:00".into() },
            JobLog { so_hoa_don: "INV/2".into(), khach: None, trang_thai: "loi".into(), luc: "10:01".into() },
        ]};
        let vm = build_view_model(&cfg(), &t);
        assert_eq!(vm.jobs.len(), 2);
        assert_eq!(vm.jobs[0].badge, "Đã in"); assert!(vm.jobs[0].da_in);
        assert_eq!(vm.jobs[0].khach, "Anh A");
        assert_eq!(vm.jobs[1].badge, "Lỗi"); assert!(!vm.jobs[1].da_in);
        assert_eq!(vm.jobs[1].khach, "");
    }
}
```

- [ ] **Step 2: Chạy test fail**

Run: `cargo test view_model` — Expected: FAIL (build_view_model chưa có).

- [ ] **Step 3: Viết implementation**

```rust
// SPDX-License-Identifier: AGPL-3.0-or-later
//! Logic THUẦN map trạng thái/config → dữ liệu hiển thị. Tách khỏi Slint để test
//! không cần render. ui.rs gọi build_view_model rồi bơm vào Slint properties.
use crate::config::Config;
use crate::state::TrangThaiChung;

pub struct JobRow {
    pub so_hoa_don: String,
    pub khach: String,
    pub badge: String,
    pub da_in: bool,
}

pub struct ViewModel {
    pub trang_thai_text: String,
    pub da_noi: bool,
    pub server: String,
    pub may_in: String,
    pub jobs: Vec<JobRow>,
}

pub fn build_view_model(cfg: &Config, t: &TrangThaiChung) -> ViewModel {
    ViewModel {
        trang_thai_text: if t.da_noi { "Đã kết nối".into() } else { "Mất kết nối".into() },
        da_noi: t.da_noi,
        server: cfg.server_url.clone(),
        may_in: format!("{} · {}", cfg.printer_name, cfg.paper_size),
        jobs: t.jobs.iter().map(|j| JobRow {
            so_hoa_don: j.so_hoa_don.clone(),
            khach: j.khach.clone().unwrap_or_default(),
            badge: if j.trang_thai == "da_in" { "Đã in".into() } else { "Lỗi".into() },
            da_in: j.trang_thai == "da_in",
        }).collect(),
    }
}
```

- [ ] **Step 4: Chạy test pass**

Run: `cargo test view_model` — Expected: PASS 3 test.

- [ ] **Step 5: Commit**

```bash
git add src/view_model.rs src/main.rs
git commit -m "feat(ui): view_model thuần map trạng thái→hiển thị (TDD)"
```

---

## Task 2: Cargo.toml + build.rs + ui/print-agent.slint — dựng UI khai báo, cargo check pass

**Files:**
- Modify: `Cargo.toml`
- Create: `build.rs`
- Create: `ui/print-agent.slint`

**Interfaces:**
- Produces: Slint component `MainWindow` với properties: `da_noi: bool`, `trang_thai_text: string`, `server: string`, `may_in: string`, `jobs: [{so_hoa_don: string, khach: string, badge: string, da_in: bool}]`, form fields `f_server/f_ma_shop/f_may_in/f_tray: string`, callbacks `luu()`, `in_thu()`. Slint sinh struct Rust `MainWindow` + setter `set_*` + `on_*`.

- [ ] **Step 1: Sửa Cargo.toml** — bỏ eframe/egui/wgpu, thêm slint + build-dep + windows.

```toml
[dependencies]
rust_socketio = "0.6"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
base64 = "0.22"
anyhow = "1"
slint = "1.8"
tray-icon = "0.24"
image = { version = "0.25", default-features = false }

[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = ["Win32_UI_WindowsAndMessaging", "Win32_Foundation"] }

[build-dependencies]
slint-build = "1.8"
```

- [ ] **Step 2: Tạo build.rs**

```rust
fn main() {
    slint_build::compile("ui/print-agent.slint").expect("compile ui.slint");
}
```

- [ ] **Step 3: Tạo ui/print-agent.slint** (theo print-agent.pen — nội dung đầy đủ)

```slint
import { Button, LineEdit, ScrollView } from "std-widgets.slint";

export struct JobRow { so_hoa_don: string, khach: string, badge: string, da_in: bool }

export component MainWindow inherits Window {
    title: "Incokit Print Agent";
    width: 400px;
    icon: @image-url("../assets/logo.png");
    default-font-family: "Be Vietnam Pro";
    background: #F4F6F9;

    in property <bool> da_noi: false;
    in property <string> trang_thai_text: "Mất kết nối";
    in property <string> server: "";
    in property <string> may_in: "";
    in property <[JobRow]> jobs: [];
    in-out property <string> f_server: "";
    in-out property <string> f_ma_shop: "";
    in-out property <string> f_may_in: "";
    in-out property <string> f_tray: "";
    callback luu();
    callback in_thu();

    VerticalLayout {
        padding: 16px;
        spacing: 14px;

        // Titlebar
        HorizontalLayout {
            alignment: space-between;
            Text { text: "Incokit Print Agent"; font-size: 14px; font-weight: 600; color: #1B2432; vertical-alignment: center; }
            Rectangle {
                background: root.da_noi ? #E7F6EC : #FCE8E8;
                border-radius: 16px;
                horizontal-stretch: 0;
                HorizontalLayout {
                    padding-left: 10px; padding-right: 10px; spacing: 6px;
                    Text {
                        text: "● " + root.trang_thai_text;
                        color: root.da_noi ? #16A34A : #C0392B;
                        font-size: 12px; font-weight: 600; vertical-alignment: center;
                    }
                }
            }
        }

        // Status card
        Rectangle {
            background: white; border-radius: 10px; border-width: 1px; border-color: #E2E6EC;
            VerticalLayout {
                padding: 14px; spacing: 10px;
                HorizontalLayout { alignment: space-between;
                    Text { text: "Server"; color: #69727F; font-size: 13px; }
                    Text { text: root.server; color: #1B2432; font-size: 13px; font-weight: 500; }
                }
                HorizontalLayout { alignment: space-between;
                    Text { text: "Máy in"; color: #69727F; font-size: 13px; }
                    Text { text: root.may_in; color: #1B2432; font-size: 13px; font-weight: 500; }
                }
            }
        }

        // In gần đây
        Text { text: "IN GẦN ĐÂY"; color: #69727F; font-size: 12px; font-weight: 600; }
        Rectangle {
            background: white; border-radius: 10px; border-width: 1px; border-color: #E2E6EC;
            height: 160px;
            ScrollView {
                VerticalLayout {
                    for j in root.jobs: Rectangle {
                        height: 44px;
                        HorizontalLayout {
                            padding: 11px; spacing: 10px; alignment: space-between;
                            VerticalLayout {
                                Text { text: j.so_hoa_don; color: #1B2432; font-size: 13px; font-weight: 500; }
                                Text { text: j.khach; color: #69727F; font-size: 11px; }
                            }
                            Rectangle {
                                background: j.da_in ? #E7F6EC : #FCE8E8; border-radius: 12px;
                                HorizontalLayout { padding-left: 9px; padding-right: 9px;
                                    Text { text: j.badge; color: j.da_in ? #16A34A : #C0392B; font-size: 11px; font-weight: 600; vertical-alignment: center; }
                                }
                            }
                        }
                    }
                }
            }
        }

        // Cấu hình
        Text { text: "CẤU HÌNH"; color: #69727F; font-size: 12px; font-weight: 600; }
        Rectangle {
            background: white; border-radius: 10px; border-width: 1px; border-color: #E2E6EC;
            VerticalLayout {
                padding: 14px; spacing: 10px;
                Text { text: "Server URL"; color: #69727F; font-size: 11px; }
                LineEdit { text <=> root.f_server; }
                Text { text: "Mã shop"; color: #69727F; font-size: 11px; }
                LineEdit { text <=> root.f_ma_shop; }
                Text { text: "Máy in"; color: #69727F; font-size: 11px; }
                LineEdit { text <=> root.f_may_in; }
                Text { text: "Khay"; color: #69727F; font-size: 11px; }
                LineEdit { text <=> root.f_tray; }
            }
        }

        // Actions
        HorizontalLayout {
            spacing: 10px;
            Button { text: "In thử"; clicked => { root.in_thu(); } }
            Button { text: "Lưu"; primary: true; clicked => { root.luu(); } }
        }
    }
}
```

- [ ] **Step 4: cargo check trên máy Win (Mac không có windows crate target)**

Chạy qua WinRM: `cd <repo>; cargo check 2>&1 | Select-Object -Last 10`
Expected: compile OK (có thể cảnh báo unused vì ui.rs chưa nối) — KHÔNG lỗi .slint syntax.
Nếu lỗi asset logo.png thiếu: tạm bỏ dòng `icon:` hoặc thêm assets/logo.png.

- [ ] **Step 5: Commit**

```bash
git add Cargo.toml build.rs ui/print-agent.slint
git commit -m "feat(ui): dựng UI Slint theo thiết kế Pencil + đổi deps egui→slint"
```

---

## Task 3: taskbar_win — Win32 ẩn cửa sổ khỏi taskbar (Windows)

**Files:**
- Create: `src/taskbar_win.rs`
- Modify: `src/main.rs` (thêm `mod taskbar_win;`)

**Interfaces:**
- Produces: `pub fn an_khoi_taskbar(hwnd: isize)` — set WS_EX_TOOLWINDOW + bỏ WS_EX_APPWINDOW cho cửa sổ, dùng trick hide→setstyle→show. No-op trên non-Windows.

- [ ] **Step 1: Viết taskbar_win.rs**

```rust
// SPDX-License-Identifier: AGPL-3.0-or-later
//! Ẩn cửa sổ khỏi taskbar Windows (tray-only). Slint/winit không có API sẵn nên
//! phải xuống Win32: đổi extended style sang WS_EX_TOOLWINDOW. CreateWindow đã tạo
//! taskbar button rồi nên phải hide→đổi style→show để nút biến mất (MSDN).
#[cfg(windows)]
pub fn an_khoi_taskbar(hwnd: isize) {
    use windows::Win32::Foundation::HWND;
    use windows::Win32::UI::WindowsAndMessaging::{
        GetWindowLongPtrW, SetWindowLongPtrW, ShowWindow, GWL_EXSTYLE,
        WS_EX_TOOLWINDOW, WS_EX_APPWINDOW, SW_HIDE, SW_SHOW,
    };
    let h = HWND(hwnd as *mut core::ffi::c_void);
    unsafe {
        let mut ex = GetWindowLongPtrW(h, GWL_EXSTYLE);
        ex |= WS_EX_TOOLWINDOW.0 as isize;
        ex &= !(WS_EX_APPWINDOW.0 as isize);
        let _ = ShowWindow(h, SW_HIDE);
        SetWindowLongPtrW(h, GWL_EXSTYLE, ex);
        let _ = ShowWindow(h, SW_SHOW);
    }
}

#[cfg(not(windows))]
pub fn an_khoi_taskbar(_hwnd: isize) {}
```

- [ ] **Step 2: cargo check trên Win**

Run (WinRM): `cargo check 2>&1 | Select-Object -Last 8` — Expected: compile OK (windows crate features đúng).

- [ ] **Step 3: Commit**

```bash
git add src/taskbar_win.rs src/main.rs
git commit -m "feat(ui): Win32 ẩn cửa sổ khỏi taskbar (tray-only)"
```

(Verify ẩn thật ở Task 5 khi có window chạy — nhìn taskbar RDP.)

---

## Task 4: config.rs — token thành hằng nhúng, "mã shop"=org_id

**Files:**
- Modify: `src/config.rs`

**Interfaces:**
- Consumes: config.ini format cũ (server_url, org_id, printer_name, tray, paper_size — bỏ token khỏi file).
- Produces: `pub const AGENT_TOKEN: &str` (hằng nhúng); `Config.token` vẫn tồn tại (net.rs dùng) nhưng gán = AGENT_TOKEN khi load, không đọc từ file.

- [ ] **Step 1: Đọc config.rs hiện tại** để biết parse_config + ghi_config, giữ nguyên field Config (net.rs phụ thuộc `token`/`org_id`).

- [ ] **Step 2: Sửa** — thêm `pub const AGENT_TOKEN: &str = "<token thật từ AI_MAY_IN_AGENT_TOKEN>";` (lấy giá trị thật từ backend .env). parse_config: nếu file KHÔNG có `token`, gán `token: AGENT_TOKEN.to_string()`. ghi_config: KHÔNG ghi token ra file.

```rust
// Trong config.rs, thêm đầu file:
pub const AGENT_TOKEN: &str = "PLACEHOLDER_LẤY_TỪ_BACKEND_AI_MAY_IN_AGENT_TOKEN";

// Trong parse_config, sau khi đọc các field, ép token:
// (nếu file có token thì vẫn nhận để tương thích, không có thì dùng hằng)
let token = section_token.unwrap_or_else(|| AGENT_TOKEN.to_string());
```

Lưu ý: giá trị token thật lấy từ `AI_MAY_IN_AGENT_TOKEN` trên backend prod (WinRM/ssh đọc), KHÔNG hard-code bừa. Nếu chưa lấy được → để hằng đọc từ env `AGENT_TOKEN` lúc build (`option_env!`).

- [ ] **Step 3: Test** (config test cũ giữ pass) — `cargo test config` trên Win.

- [ ] **Step 4: Commit**

```bash
git add src/config.rs
git commit -m "feat(config): token thành hằng nhúng, mã shop=org_id (UI 1 ô)"
```

---

## Task 5: ui.rs — viết lại bằng Slint (window + tray + vòng lặp), verify trên Win/RDP

**Files:**
- Rewrite: `src/ui.rs`
- Modify: `src/main.rs` (giữ `ui::chay_ui(cfg, trang_thai)` — không đổi)

**Interfaces:**
- Consumes: `MainWindow` (Slint, Task 2), `build_view_model` (Task 1), `an_khoi_taskbar` (Task 3), `config::{Config, AGENT_TOKEN, ghi_config}`, `net::chay_net`, `printing::in_pdf`, `TrangThaiChung`, `tray-icon`.
- Produces: `pub fn chay_ui(cfg: Arc<Config>, trang_thai: Arc<Mutex<TrangThaiChung>>) -> Result<(), slint::PlatformError>`.

- [ ] **Step 1: Viết ui.rs** — dựng MainWindow, ép software backend, tray icon xanh/đỏ, timer đọc trang_thai bơm vào properties, nút Lưu (ghi config + spawn net), In thử (in_pdf giả), menu tray Cấu hình/Thoát, ẩn taskbar lúc window ready, khởi động ẩn window.

(Nội dung đầy đủ — dựng theo pattern: `slint::platform` set software renderer; `MainWindow::new()`; `slint::Timer::start(Repeated, 500ms, || đọc trang_thai → set_da_noi/set_jobs...)`; tray-icon build như bản egui cũ giữ; `window.window().with_winit_window(|w| an_khoi_taskbar(hwnd))` hoặc lấy hwnd qua raw-window-handle; window.hide() lúc đầu; nút .on_luu(ghi_config + net::chay_net thread mới).)

Chi tiết code viết lúc thực thi Task (cần API Slint 1.8 chính xác — tra doc.rs/slint lúc code). Điểm khoá:
- Ép software: `SLINT_BACKEND=winit-software` (env trong main hoặc `slint::BackendSelector`).
- Lấy HWND: qua `raw_window_handle` từ Slint window (feature `raw-window-handle-06`).
- Tray: giữ nguyên tray-icon crate + MenuEvent poll trong Slint timer.

- [ ] **Step 2: Build trên Win** (WinRM): `cargo build --release` — Expected: Finished.

- [ ] **Step 3: VERIFY trên RDP** (anh Quốc nhìn màn, hoặc screenshot qua WinRM):
  - Chạy exe → cửa sổ Slint hiện đúng thiết kế Pencil (card, chip, list job, form, 2 nút), font Việt CÓ DẤU.
  - Icon khay xuất hiện, xanh khi nối / đỏ khi mất.
  - **Cửa sổ KHÔNG hiện ở taskbar + KHÔNG trong Alt+Tab.**
  - Bấm menu tray "Cấu hình..." → cửa sổ hiện. Nút X → ẩn về tray. "Thoát" → thoát.
  - Nút Lưu → ghi config.ini + nối lại (chip chuyển xanh). In thử → máy in nhả giấy (hoặc dry-run).

- [ ] **Step 4: Commit**

```bash
git add src/ui.rs src/main.rs
git commit -m "feat(ui): viết lại UI bằng Slint software-render, tray-only ẩn taskbar"
```

---

## Task 6: Dọn + push

- [ ] **Step 1: Xoá dấu vết egui** — grep còn `egui`/`eframe`/`wgpu` nào trong src không, xoá. Xoá assets font nếu Slint dùng font khác (giữ nếu vẫn dùng).
- [ ] **Step 2: `cargo build --release` + `cargo test` trên Win** — tất cả pass.
- [ ] **Step 3: Verify e2e cuối trên RDP** (checklist Task 5 Step 3) — anh Quốc xác nhận qua ảnh.
- [ ] **Step 4: Push**

```bash
git push origin feat/ui-slint
```
(Máy Win/shop: git pull + cargo build --release.)

---

## Self-review

- **Spec coverage:** §4 UI→Task 2 (.slint theo Pencil); §5 config token/mã shop→Task 4; §6 kiến trúc thread→Task 5; §7 hành vi giữ→Task 5; §8 verify Win→Task 5 Step 3; §2 giữ lõi→không task nào đụng net/printing/state/job ✓.
- **Placeholder:** Task 4 AGENT_TOKEN để "PLACEHOLDER" — CÓ CHỦ Ý (giá trị thật lấy từ backend lúc thực thi, không bịa). Task 5 Step 1 code chi tiết hoãn tới lúc code vì cần API Slint 1.8 chính xác — chấp nhận (render code khó viết mù, tra doc lúc làm).
- **Type nhất quán:** ViewModel/JobRow (Task 1) khớp property MainWindow (Task 2) khớp set trong ui.rs (Task 5) ✓. chay_ui chữ ký giữ nguyên ✓.
